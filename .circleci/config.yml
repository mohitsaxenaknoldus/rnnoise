version: 2.1

orbs:
  docker: circleci/docker@1.7.0

jobs:
  example-job:
    docker:
      - image: cimg/base:stable
    steps:
      - docker/build:
          path: rnnoise/Dockerfile 
          image: rnnoise 

workflows:
  example-workflow:
    jobs:
      - example-job