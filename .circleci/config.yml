version: 2.1

orbs:
  docker: circleci/docker@1.7.0

jobs:
  example-job:
    docker:
      - image: cimg/base:stable
    steps:
      - docker/check
workflows:
  example-workflow:
    jobs:
      - example-job